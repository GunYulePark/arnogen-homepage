'use client';
import {useEffect,useState} from 'react';import {navigation} from '@/content/navigation';import {sitePath} from '@/lib/site-path';
export function Header(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>12);onScroll();window.addEventListener('scroll',onScroll,{passive:true});return()=>window.removeEventListener('scroll',onScroll)},[]);return <header className={`site-header ${scrolled?'is-scrolled':''}`}><div className="wrap header-inner"><a href={sitePath()} className="brand" aria-label="아르노젠 홈">ARNO<span className="brand-dot"/>ZEN</a><nav className="desktop-nav" aria-label="주요 메뉴">{navigation.map(group=><div className="nav-group" key={group.label}><a className="nav-trigger" href={sitePath(group.items[0].href)}>{group.label}</a><div className="submenu">{group.items.map(item=><a href={sitePath(item.href)} key={item.href}>{item.label}</a>)}</div></div>)}<a href={sitePath('/contact')} className="nav-contact">Contact Us</a></nav><button className="mobile-toggle" aria-expanded={open} aria-controls="mobile-menu" onClick={()=>setOpen(!open)}>{open?'×':'☰'}<span className="sr-only">메뉴</span></button><nav id="mobile-menu" className={`mobile-panel ${open?'open':''}`} aria-label="모바일 메뉴">{navigation.flatMap(g=>g.items).map(item=><a href={sitePath(item.href)} onClick={()=>setOpen(false)} key={item.href}>{item.label}</a>)}<a href={sitePath('/contact')} onClick={()=>setOpen(false)}>문의하기</a></nav></div></header>}
