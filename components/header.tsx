'use client';
import Link from 'next/link';import {useState} from 'react';import {navigation} from '@/content/navigation';
export function Header(){const[open,setOpen]=useState(false);return <header className="site-header"><div className="wrap header-inner"><Link href="/" className="brand" aria-label="아르노젠 홈">ARNO<span className="brand-dot"/>ZEN</Link><nav className="desktop-nav" aria-label="주요 메뉴">{navigation.map(group=><div className="nav-group" key={group.label}><Link className="nav-trigger" href={group.items[0].href}>{group.label}</Link><div className="submenu">{group.items.map(item=><Link href={item.href} key={item.href}>{item.label}</Link>)}</div></div>)}<Link href="/contact" className="nav-contact">Contact Us</Link></nav><button className="mobile-toggle" aria-expanded={open} aria-controls="mobile-menu" onClick={()=>setOpen(!open)}>{open?'×':'☰'}<span className="sr-only">메뉴</span></button><nav id="mobile-menu" className={`mobile-panel ${open?'open':''}`} aria-label="모바일 메뉴">{navigation.flatMap(g=>g.items).map(item=><Link href={item.href} onClick={()=>setOpen(false)} key={item.href}>{item.label}</Link>)}<Link href="/contact" onClick={()=>setOpen(false)}>문의하기</Link></nav></div></header>}
